# Basic league component for ESL

This component has a property for league ID and fetches results and contestants automatically from API.

The component has also the option for changing the sort algorithm from ascending to descending and vice versa.

## Installation

To install, just download and run the following command

``npm i``

To see it working, start a local server

``npm start``